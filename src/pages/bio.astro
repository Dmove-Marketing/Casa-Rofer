---
import Base from '../layouts/Base.astro';
import config from '../../config.json';
import LeadForm from '../components/forms/LeadForm.astro';

const bioFields = [
  { type: 'row', fields: [
    { name: 'nome',     type: 'text',   label: 'Nome',     required: true },
    { name: 'telefone', type: 'text',   label: 'Telefone', required: true },
  ]},
  { name: 'email', type: 'email', label: 'E-mail', required: true },
  { type: 'row', fields: [
    { name: 'data',       type: 'text',   label: 'Data do evento', placeholder: 'dd/mm/aaaa', datepicker: true },
    { name: 'convidados', type: 'number', label: 'Número de convidados' },
  ]},
  { name: 'tipo_evento', type: 'select', label: 'Tipo de evento', options: [
    { value: '', disabled: true, selected: true, label: 'Selecione o tipo de evento' },
    { value: 'Eventos corporativos', label: 'Eventos corporativos' },
    { value: 'Casamento', label: 'Casamento' },
    { value: 'Bodas', label: 'Bodas' },
    { value: 'Aniversario', label: 'Aniversário' },
    { value: 'Debutante', label: 'Debutante' },
    { value: 'Outros', label: 'Outros' }
  ]},
  { name: 'detalhes', type: 'textarea', label: 'Detalhes adicionais', rows: 3 }
];
---

<Base
  title="Casa Rofer — Orçamento | Brooklin, São Paulo"
  description="Fale conosco e solicite um orçamento personalizado para sua celebração ou evento corporativo."
  ogImage="https://media.dmove.com.br/clients/casa-rofer/photos/bio-casa-rofer.webp"
>
  <main class="bio-container">
    <div class="bio-content">
      <div class="bio-header">
        <img class="bio-logo" src="https://media.dmove.com.br/clients/casa-rofer/photos/logo-white.webp" alt="Casa Rofer" />
        <h1 class="bio-title">A arte de transformar<br><span class="accent">o instante em memórias</span></h1>
      </div>

      <div class="bio-card">
        <LeadForm
          formId="bio-form"
          project={config.project_slug}
          fonte="Landing page/bio"
          submitUrl={config.forms['lead-form'].webhooks[0]}
          redirectUrl={config.forms['lead-form'].redirect_on_success}
          submitText="Solicitar Orçamento"
          honeypot
          fields={bioFields}
          successTitle="Recebemos sua solicitação ✦"
          successText="Obrigado pelo contato. Em breve nossa equipe retornará para começar a planejar o seu evento."
        />
      </div>
      <footer class="bio-footer">
        <span>© 2026 Casa Rofer · Brooklin, São Paulo. Todos os direitos reservados.</span>
        <span style="display:inline-flex;align-items:center;justify-content:center;gap:8px">Desenvolvido por <a href="https://www.dmove.com.br" target="_blank" rel="noopener" style="display:inline-flex;align-items:center"><img src="https://media.dmove.com.br/clients/casa-rofer/photos/Logotipo-dmove.webp" alt="Dmove" style="height:30px;width:auto;opacity:0.95" /></a></span>
      </footer>
    </div>
  </main>
</Base>

<style>
  /* ===== FONTS IMPORT (Para itálicos PP Editorial New) ===== */
  @font-face {
    font-family: 'PP Editorial New';
    src: url('/src/assets/fonts/PPEditorialNew-Italic.woff2') format('woff2');
    font-weight: 400;
    font-style: italic;
    font-display: swap;
  }

  .accent {
    font-family: 'PP Editorial New', serif;
    font-style: italic;
    font-weight: 400;
  }

  /* ===== LAYOUT & BG ===== */
  .bio-container {
    min-height: 100vh;
    width: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: clamp(20px, 5vw, 60px) 16px;
    background: linear-gradient(rgba(2, 61, 45, 0.48), rgba(2, 61, 45, 0.8)), 
                url('https://media.dmove.com.br/clients/casa-rofer/photos/bio-casa-rofer.webp') no-repeat center center;
    background-size: cover;
    background-attachment: fixed;
    position: relative;
    overflow-y: auto;
  }

  /* ===== WRAPPER/CONTENT ===== */
  .bio-content {
    width: 100%;
    max-width: 620px;
    margin: auto;
    display: flex;
    flex-direction: column;
    gap: clamp(20px, 4vw, 36px);
  }

  /* ===== CARD ===== */
  .bio-card {
    width: 100%;
    background: rgba(255, 255, 255, 0.08);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border: 1px solid rgba(255, 255, 255, 0.16);
    border-radius: 16px;
    padding: clamp(24px, 6vw, 48px);
    padding-top: clamp(16px, 4vw, 24px);
    box-shadow: 0 24px 80px rgba(0, 0, 0, 0.45);

    /* Sobrescrita de variáveis CSS do Astro para o LeadForm */
    --ink: #ffffff;
    --ink-soft: rgba(255, 255, 255, 0.7);
    --line: rgba(255, 255, 255, 0.25);
    --title: #ffffff;
    --bg-card: transparent;
    --sand: #ada28f;
    --sand-deep: #ffffff;
  }

  /* ===== HEADER ===== */
  .bio-header {
    text-align: center;
    margin-bottom: clamp(20px, 4vw, 36px);
  }

  .bio-logo {
    width: 180px;
    height: auto;
    margin: 0 auto clamp(16px, 3vw, 24px);
    display: block;
  }

  .bio-title {
    font-family: 'Aktiv Grotesk', sans-serif;
    font-weight: 400;
    font-size: clamp(1.9rem, 4vw, 3.1rem);
    line-height: 0.85;
    color: #ffffff;
    letter-spacing: 0.02em;
  }

  /* ===== FOOTER ===== */
  .bio-footer {
    text-align: center;
    font-family: 'Aktiv Grotesk', sans-serif;
    font-size: 0.78rem;
    opacity: 0.6;
    color: #ffffff;
    display: flex;
    flex-direction: column;
    gap: 8px;
    margin-top: 12px;
  }
  .bio-footer a {
    color: var(--sand);
    text-decoration: none;
    font-weight: 500;
  }
  .bio-footer a:hover {
    text-decoration: underline;
  }

  /* ===== CUSTOM ADJUSTMENTS FOR WHITE FORM ===== */
  .bio-card :global(.form-submit) {
    background: var(--sand);
    color: #ffffff;
    font-weight: 600;
    border: 1px solid var(--sand);
    transition: background 0.3s, color 0.3s, border-color 0.3s;
  }

  .bio-card :global(.form-submit:hover) {
    background: transparent;
    color: #ffffff;
    border-color: #ffffff;
    box-shadow: none;
  }

  .bio-card :global(.form-select) {
    color: #ffffff;
  }

  .bio-card :global(.form-select option) {
    background: #023d2d;
    color: #ffffff;
  }

  .bio-card :global(.form-success-icon) {
    background: var(--sand) !important;
    color: #023d2d !important;
  }

  .bio-card :global(.form-success-icon svg) {
    stroke: #023d2d !important;
  }

  /* ===== FLATPICKR — tema Casa Rofer (verde + areia) ===== */
  :global(.flatpickr-calendar) {
    background: linear-gradient(180deg, rgba(6, 71, 53, 0.98), rgba(2, 61, 45, 0.98));
    backdrop-filter: blur(24px);
    -webkit-backdrop-filter: blur(24px);
    border: 1px solid rgba(255, 255, 255, 0.14);
    border-radius: 18px;
    box-shadow: 0 30px 70px rgba(0, 0, 0, 0.5), inset 0 1px 0 rgba(255, 255, 255, 0.06);
    padding: 18px;
    width: auto !important;
    margin-top: 12px !important;
    animation: fp-fade-in .22s ease-out;
  }

  @keyframes fp-fade-in {
    from { opacity: 0; transform: translateY(-6px); }
    to   { opacity: 1; transform: translateY(0); }
  }

  :global(.flatpickr-calendar.arrowTop:before),
  :global(.flatpickr-calendar.arrowTop:after),
  :global(.flatpickr-calendar.arrowBottom:before),
  :global(.flatpickr-calendar.arrowBottom:after) {
    display: none;
  }

  :global(.flatpickr-months) {
    display: flex;
    align-items: center;
    padding-bottom: 14px;
    margin-bottom: 12px;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  }

  :global(.flatpickr-month) {
    color: #ffffff;
    fill: #ffffff;
    height: auto;
  }

  :global(.flatpickr-current-month) {
    font-family: 'PP Editorial New', serif;
    font-style: italic;
    font-weight: 400;
    font-size: 1.15rem;
    color: #ffffff;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 6px;
    padding: 0;
  }

  :global(.flatpickr-current-month .cur-month) {
    font-family: 'PP Editorial New', serif;
    font-style: italic;
  }

  :global(.flatpickr-current-month input.cur-year) {
    color: rgba(255, 255, 255, 0.75);
    font-family: 'Aktiv Grotesk', sans-serif;
    font-style: normal;
    font-size: 0.95rem;
    font-weight: 400;
  }

  :global(.flatpickr-current-month .flatpickr-monthDropdown-months) {
    background: transparent;
    color: #ffffff;
    font-family: 'PP Editorial New', serif;
    font-style: italic;
    appearance: none;
    border: none;
  }

  :global(.flatpickr-current-month .flatpickr-monthDropdown-months option) {
    background: #023d2d;
    color: #ffffff;
    font-style: normal;
  }

  :global(.numInputWrapper span.arrowUp),
  :global(.numInputWrapper span.arrowDown) {
    display: none;
  }

  :global(.flatpickr-prev-month),
  :global(.flatpickr-next-month) {
    fill: rgba(255, 255, 255, 0.55);
    color: rgba(255, 255, 255, 0.55);
    width: 30px;
    height: 30px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: background-color .25s, fill .25s, color .25s;
  }

  :global(.flatpickr-prev-month svg),
  :global(.flatpickr-next-month svg) {
    width: 14px;
    height: 14px;
  }

  :global(.flatpickr-prev-month:hover),
  :global(.flatpickr-next-month:hover) {
    background: rgba(255, 255, 255, 0.08);
    fill: var(--sand);
    color: var(--sand);
  }

  :global(.flatpickr-weekdays) {
    background: transparent;
    margin-bottom: 6px;
  }

  :global(span.flatpickr-weekday) {
    background: transparent;
    color: rgba(255, 255, 255, 0.4);
    font-family: 'Aktiv Grotesk', sans-serif;
    font-size: 0.64rem;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    font-weight: 500;
  }

  :global(.flatpickr-day) {
    color: rgba(255, 255, 255, 0.82);
    border-radius: 50%;
    font-family: 'Aktiv Grotesk', sans-serif;
    font-size: 0.86rem;
    height: 36px;
    line-height: 36px;
    max-width: 36px;
    margin: 1px 0;
  }

  :global(.flatpickr-day.today) {
    border-color: var(--sand);
    color: #ffffff;
  }

  :global(.flatpickr-day.today:hover) {
    border-color: var(--sand);
  }

  :global(.flatpickr-day:hover) {
    background: rgba(255, 255, 255, 0.1);
    border-color: transparent;
    color: #ffffff;
  }

  :global(.flatpickr-day.selected),
  :global(.flatpickr-day.selected:hover) {
    background: var(--sand);
    border-color: var(--sand);
    color: #023d2d;
    font-weight: 600;
    box-shadow: 0 6px 18px rgba(173, 162, 143, 0.4);
  }

  :global(.flatpickr-day.flatpickr-disabled),
  :global(.flatpickr-day.flatpickr-disabled:hover) {
    color: rgba(255, 255, 255, 0.6);
    cursor: not-allowed;
  }

  :global(.flatpickr-day.prevMonthDay),
  :global(.flatpickr-day.nextMonthDay) {
    color: rgba(255, 255, 255, 0.25);
  }
</style>
